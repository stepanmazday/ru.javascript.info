Строки
В JavaScript текстовые данные хранятся в виде строк. Отдельного типа данных для одного символа в JavaScript нет.

Внутренний формат для строк UTF-16, вне зависимости от кодировки страницы.

Строка может быть заключена в одинарные кавычки, двойные кавычки или одиночные кавычки:

let single = 'одинарные кавычки';
let double = "двойные кавычки";
let backticks = `одиночные кавычки`;

Одинарные и двойные кавычки равнозначны. Одиночные, позволяют встраивать в строку любые выражения, в том числе и функции:

function sum(a, b) {
  return a + b;
}
alert(`1 + 2 = ${sum(1, 2)}.`); // 1 + 2 = 3

Еще одним преимуществом использования одиночных кавычек является то, что они позволяют строке содержать несколько строк:

let guestList = `Guests:
 * John
 * Pete
 * Mary
`;
alert(guestList); // в переменной guestList, несколько строк

Если попытаться использовать одинарные или двойные кавычки, это приведет к ошибке:

let guestList = "Guests:  // Error: Unexpected token ILLEGAL
  * John";

Одинарные и двойные кавычки применялись в языке JavaScript когда необходимость в многострочных строках не учитывалась. Одиночные кавычки появились позже и поэтому являются более универсальными.

Одиночные кавычки также позволяют нам задать "шаблонную функцию"   func`string`. Функция вызывается автоматически, получает строку и встроенные выражения и может обрабатывать их. Подробнее об этом можно прочитать в документации. Это называется «пользовательские тэги». Эта функция облегчает перенос строк в пользовательских шаблонах, но используется редко.

Специальные символы

По-прежнему можно создавать многострочные строки, используя специальный символ \n, который обозначает разрыв строки:

let guestList = "Guests:\n * John\n * Pete\n * Mary";

alert(guestList); // многострочная строка guests

Например, эти две строки равнозначны:

alert( "Hello\nWorld" ); 

alert( `Hello
World` );

Есть и другие, менее распространенные специальные символы. 

Character
Description
\b
Возврат на шаг
\f
перевод строки
\n
Новая строка
\r
Возврат каретки
\t
табуляция
\uNNNN
Символ Unicode с шестнадцатеричным кодом NNNN, например, \u00A9 -- это символ Unicode  для символа авторского права ©. Должно быть, ровно 4 цифры.
\u{NNNNNNNN}
Некоторые редкие символы кодируются двумя символами Unicode, занимающими до 4 байт. Этот длинный Unicode требует фигурных скобок вокруг него.

Примеры с Unicode:
alert( "\u00A9" ); // ©
alert( "\u{20331}" ); // 佫  редкий китайский иероглиф (длинный Unicode)
alert( "\u{1F60D}" ); // 😍 смайлик (еще один длинный Unicode)


Все специальные символы начинаются с символа обратной косой черты \. Он также называется обратным слешем.

Можно использовать его, если нужно вставить в строку кавычки.

alert( 'I*!*\'*/!*m the Walrus!' ); // *!*I'm*/!* the Walrus!

Необходимо поставить кавычку после обратного слеша \', иначе он будет указывать на конец строки.

Можно также использовать двойные кавычки или одинарные кавычки:

alert( `I'm the Walrus!` ); // I'm the Walrus!

А если нужно показать обратный слеш \ в строке?

Это возможно, но нужно удвоить его:

alert( `обратный слеш: \\` ); // обратный слеш: \

Длина строки

Свойство length содержит длину строки:

alert( `My\n`.length ); // 3

Обратите внимание, что \n является специальным символом, поэтому длина строки равна 3.

Обратите внимание, что str.length - это числовое свойство, а не функция. Нет необходимости добавлять после него скобки.

Получение символов
Чтобы получить символ в позиции `pos`, используйте квадратные скобки `[pos]` или вызовите метод [str.charAt(pos)]. Первый символ начинается с нулевой позиции:

let str = `Hello`;
// первый символ
alert( str[0] ); // H
alert( str.charAt(0) ); // H
// последний символ
alert( str[str.length - 1] ); // o

Квадратные скобки - это современный способ получить символ, в то время как charAt существует для совместимости со старым кодом.

Единственное различие между ними заключается в том, что если символ не найден, [] возвращает undefined, а charAt возвращает пустую строку:

let str = `Hello`;
alert( str[1000] ); // undefined
alert( str.charAt(1000) ); // '' пустая строка

Можно вывести символы по одному, используя цикл for..of:

for (let char of "Hello") {
  alert(char); // H,e,l,l,o (char becomes "H", then "e", then "l" etc)
}

Изменение строки

Строка не может быть изменена в JavaScript c помощью изменения одного символа.

let str = 'Hi';
str[0] = 'h'; // ошибка
alert( str[0] ); // не сработает

Нужно создать новую строку и присвоить этой строке новое значение.

let str = 'Hi';
str = 'h' + str[1];  // замена строки
alert( str ); // hi

Методы toLowerCase() и toUpperCase() меняют регистр:

alert( 'Interface'.toUpperCase() ); // INTERFACE
alert( 'Interface'.toLowerCase() ); // interface

Если хотим, чтобы один символ был в нижнем регистре:

alert( 'Interface'[0].toLowerCase() ); // 'i'

Поиск подстроки

Существует несколько способов поиска подстроки в строке.
Первый метод str.indexOf(substr, pos).

Он ищет подcтроку в str, начиная с заданной позиции pos, и возвращает номер позиции, где найдено совпадение, или -1, если ничего не найдено.

let str = 'Widget with id';
alert(str.indexOf('Widget')); // 0, потому что в начале найден 'Widget'
alert(str.indexOf('widget')); //-1, не найдено (поиск чувствителен к регистру)
alert(str.indexOf("id")); // 1, "id" находится в позиции 1 (...idget with id)

Второй дополнительный параметр метода indexOf позволяет нам осуществлять поиск, начиная с заданной позиции.

Например, первое совпадение с "id" находится в позиции 1. Чтобы найти следующее совпадение, начнем поиск с позиции 2:

let str = 'Widget with id';
alert( str.indexOf('id', 2)) // 12

Если нужно найти все совпадения, можно использовать indexOf в цикле. 

let str = 'Хитрый, как лиса, сильный, как бык';
let target = 'как'; // давайте поищем подстроку «как».
let pos = 0;
while (true) {
  let foundPos = str.indexOf(target, pos);
  if (foundPos == -1) break;
  alert( `Found at ${foundPos}` );
  pos = foundPos + 1; // продолжить поиск со следующей позиции.
}

Можно написать короче:

let str = "Хитрый, как лиса, сильный, как бык";
let target = "как";

let pos = -1;
while ((pos = str.indexOf(target, pos + 1)) != -1) {
  alert( pos );
}

Также существует аналогичный метод str.lastIndexOf(substr, position) , который ищет от конца строки до ее начала.

Он будет искать совпадения с конца строки.

Имеются небольшие неудобства с indexOf в операторе if. 

let str = "Widget with id";
if (str.indexOf("Widget")) {
    alert("We found it"); // не сработает!
}

Предупреждение в этом примере не отображается, потому что str.indexOf("Widget") возвращает 0 (что означает, что он нашел совпадение в начальной позиции). Но оператор if вместо 0 выдаст  false.

Необходимо проверить значение -1, вот так:

let str = "Widget with id";
if (str.indexOf("Widget") != -1) {
    alert("We found it"); // теперь сработает!
}

Побитовый NOT

Один из старых трюков использование побитового оператора NOT  ~. Он преобразует число в 32-битное целое число (удаляет десятичную часть, если она существует), а затем меняет все биты в двоичном представлении.

Для 32-битных целых чисел вызов ~n означает тоже, что и вызов -(n+1) (благодаря формату IEEE-754).

Например

alert( ~2 ); // -3,  -(2+1)
alert( ~1 ); // -2,  -(1+1)
alert( ~0 ); // -1, -(0+1)
alert( ~-1 ); // 0, -(-1+1)

 ~n равен нулю, только если n = = -1 (это для любого 32-битного целого числа n).
Сравнение if ( ~str.indexOf("...") ) является правдивым только если результат indexOf не равен -1. 

Он используется его для сокращения времени проверки:

let str = "Widget";
if (~str.indexOf("Widget")) {
  alert( 'Found it!' ); // сработает
}

Обычно не рекомендуется использовать возможности языка неочевидным образом, но этот трюк широко используется в старом коде, поэтому мы должны о нем знать.

Просто помните: if (~str.indexOf(...)) читается как "если найдено".

Формально оператор ~ усекает числа до 32 бит, поэтому существуют большие числа, дающие 0, наименьшее из них ~42949677295=0. 
Этот трюк используется в старом коде, в современном JavaScript есть метод .include (см. ниже).

includes, startsWith, endsWith

Более современный метод str.includes(substr, pos) возвращает true или false в зависимости от того, содержит ли строка str подстроку substr.

Этот метод необходим если нужно провести тест на совпадение, но не нужна его позиция:

alert( "Widget with id".includes("Widget") ); // true
alert( "Hello".includes("Bye") ); // false

Необязательным вторым аргументом в Str.include является позиция, с которой можно начать поиск:

alert("Midget".includes("id") ); // true
alert("Midget".includes("id", 3) );// false, с 3 позиции нет "id"

Методы str.startWith и str.endWith необходимы для проверки нахождения подстроки в начале или конце строки str:

alert( "Widget".startsWith("Wid") ); // true, "Widget" начинается с "Wid"
alert( "Widget".endsWith("get") );   // true, "Widget" заканчивается на "get"

Получение подстроки

В JavaScript существует 3 метода в JavaScript для получения подстроки: substring, substr и slice. 
str.slice(start [, end]) : Возвращает часть строки от start до (но не включая) end.

Например

let str = "stringify";
alert( str.slice(0, 5) ); // 'strin', подстрока от 0 до 5 (не включая 5)
alert( str.slice(0, 1) ); // 's', от 0 до 1, но не включая 1, поэтому только символ при 0

Если второй аргумент отсутствует, то метод доходит до конца строки:

let str = "st*!*ringify*/!*";
alert( str.slice(2) ); // от 2-й позиции до конца

Также возможно отрицательное значение для start и end. Это значит, что позиция отсчитывается от конца строки:

let str = "strin*!*gif*/!*y";
// Начните с 4-й позиции справа, закончите с 1-й справа
alert( str.slice(-4, -1) ); // gif

Метод str.subtring(start [, end]]) возвращает часть строки между началом и концом.

Это почти то же самое, что и slice, но позволяет start быть больше, чем end.

let str = "st*!*ring*/!*ify";
// Это то же самое для substring
alert( str.substring(2, 6) ); // "ring"
alert( str.substring(6, 2) ); // "ring"
// ...но не slice:
alert( str.slice(2, 6) ); // "ring"
alert( str.slice(6, 2) ); // "" (пустая строка)

Отрицательные аргументы (в отличие от slice) не поддерживаются, они рассматриваются как 0.

Метод str.sub(start [, length]) Возвращает часть строки от start, с заданной длиной length.

В отличие от предыдущих методов, этот метод позволяет задавать не конечную позицию, а длину:
let str = "st*!*ring*/!*ify";
alert( str.substr(2, 4) ); // начиная со второй позиции, длиной 4 символа

Первый аргумент может быть отрицательным, считается с конца:

let str = "strin*!*gi*/!*fy";
alert( str.substr(-4, 2) ); // с 4 позиции с конца получаем 2 символа

Поведем итоги:

Метод
Выборка
Отрицательные значения
slice(start, end)
От start до end
да
substring(start, end)
От start до end
равны 0
substr(start, length)
Начиная со значения start   длиной length 
Только start

Сравнение строк

Строки сравниваются посимвольно в алфавитном порядке.

Хотя, есть некоторые странности.

Заглавная буква всегда больше прописной:

alert( 'a' > 'Z' ); // true

Буквы с диакритическими знаками "не в порядке":

alert( 'Österreich' > 'Zealand' ); // true

Это может привести к странным результатам, если мы отсортируем названия этих стран. Обычно люди ожидают, что Зеландия идет после Эстеррайхом в списке.Чтобы понять, что происходит, рассмотрим внутреннее представление строк в JavaScript.
Все строки закодированы с помощью UTF-16. То есть: каждый символ имеет соответствующий цифровой код. Существуют специальные методы, позволяющие получить символ для кода и обратно.
Метод str.codePointAt(pos) возвращает код символа в позиции pos:

// different case letters have different codes
alert( "z".codePointAt(0) ); // 122
alert( "Z".codePointAt(0) ); // 90

String.fromCodePoint(code)создает символ по его числовому коду

alert( String.fromCodePoint(90) ); // Z
Также можно добавлять символы Unicode по их кодам, используя `\u`, а затем шестнадцатеричный код:

// 90 - это 5a в шестнадцатеричной системе.
alert( '\u005a' ); // Z
Посмотрим на символы с кодами от 65 до 220, сделав из них строку:

let str = '';
for (let i = 65; i <= 220; i++) {
  str += String.fromCodePoint(i);
}
alert( str );
// ABCDEFGHIJKLMNOPQRSTUVWXYZ[\]^_`abcdefghijklmnopqrstuvwxyz{|}~������
// ¡¢£¤¥¦§¨©ª«¬­®¯°±²³´µ¶·¸¹º»¼½¾¿ÀÁÂÃÄÅÆÇÈÉÊËÌÍÎÏÐÑÒÓÔÕÖ×ØÙÚÛÜ

Сначала вводятся заглавные, затем несколько специальных, а затем строчные буквы.

Теперь становится понятно, почему a > Z.Символы сравниваются по их цифровому коду. Чем больше код, тем больше символ. Код для a (97) больше, чем код для Z (90).
Все строчные буквы следуют за прописными, потому что их коды больше. Некоторые буквы, такие как Ö, отличаются от основного алфавита. 

Правильное сравнение

Алгоритм сравнения строк является более сложным, чем может показаться, потому что алфавиты для разных языков различаются. Однотипная буква может располагаться по-разному в разных алфавитах.

К счастью, все современные браузеры (IE10- требует дополнительной библиотеки Intl.JS) поддерживают интернационализацию стандарта ECMA 402.

Он предоставляет специальный метод для сравнения строк на разных языках, следуя их правилам.

Функция str.localeCompare(str2):

Возвращает 1, если str больше str2 по правилам языка.
Возвращает -1, если str меньше str2.
Возвращает 0, если они равны.

Например
alert( 'Österreich'.localeCompare('Zealand') ); // -1

Фактически, этот метод имеет два дополнительных аргумента, указанных в документации, что позволяет указать язык (по умолчанию взятый из окружения) и установить дополнительные правила, такие как чувствительность к регистру или же "a" и "á" должны рассматриваться как одинаковые и т.д.

Unicode

Этот раздел будет полезен, если вы планируете иметь дело с эмодзи, редкими математическими знаками, иероглифами или другими редкими символами.

Суррогатные пары

Все часто используемые символы имеют 2-байтовые коды. Но 2 байта позволяют использовать только 65536 комбинаций, а этого недостаточно для всех возможных символов. Так редкие символы кодируются парой 2-байтовых символов под названием "суррогатная пара".

Длина таких символов составляет 2:

alert( '?'.length ); // 2, MATHEMATICAL SCRIPT CAPITAL X
alert( '?'.length ); // 2, FACE WITH TEARS OF JOY
alert( '?'.length ); // 2, a rare Chinese hieroglyph

Обратите внимание, что суррогатных пар не существовало на момент создания JavaScript, и поэтому они некорректно обрабатываются языком!

На самом деле, в каждой из вышеперечисленных строк есть по одному символу, но length показывает длину 2.

String.fromCodePoint и str.codePointAt - методы работы с суррогатными парами. Они появились недавно этом языке. До них были только String.fromCharCode и str.charCodeAt. Эти методы на самом деле такие же, как и в fromCodePoint/codePointAt, но не работают с суррогатными парами.

Получить символ может быть непросто, потому что суррогатные пары рассматриваются как два символа:

alert( '?'[0] ); // странные символы....
alert( '?'[1] ); // ....части суррогатной пары

Обратите внимание, что части суррогатной пары не имеют смысла друг без друга. Таким образом, предупреждения в примере выше действительно отображают мусор.

Технически суррогатные пары обнаруживаются по их кодам: если символ имеет код в интервале 0xd800...0xdbff, то это первая часть суррогатной пары. Следующий символ (вторая часть) должен иметь код в интервале 0xdc00...0xdfff. Эти интервалы по стандарту зарезервированы исключительно для суррогатных пар.

В вышеуказанном случае:

charCodeAt не знает о суррогатной паре, поэтому он дает коды для деталей

alert( '?'.charCodeAt(0).toString(16) ); // d835, между 0xd800 и 0xdbff
alert( '?'.charCodeAt(1).toString(16) ); // dcb3, между 0xdc00 и 0xdfff

Диакритические знаки и нормализация

Во многих языках существуют символы, состоящие из базового символа с отметкой выше/ниже него.

Например, буква a может быть базовым символом для: a á á á ä ä ã ã ā. Наиболее распространенный "композитный" символ имеет свой собственный код в таблице UTF-16. Но не все, потому что существует слишком много возможных комбинаций.

Для поддержки произвольных композиций UTF-16 позволяет использовать несколько символов Unicode. Базовый символ и один или несколько символов, которые украшают его.

Например, если за буквой S следует специальный символ "точка выше" (код \u0307), он отображается как Ṡ.

alert( 'S\u0307' ); // Ṡ

Если нам нужна дополнительная метка над буквой (или под ней) просто добавьте нужный символ метки.

Например, если мы добавим символ "точка ниже" (код \u0323), то получим "S с точками выше и ниже": Ṩ.

alert( 'S\u0307\u0323' ); // Ṩ

Это обеспечивает не только большую гибкость, но и проблему: два символа могут визуально выглядеть одинаково, но быть представлены разными композициями Unicode.
alert( 'S\u0307\u0323' ); // Ṩ, S +  точка выше + точка ниже
alert( 'S\u0323\u0307' ); // Ṩ, S +  точка ниже + точка выше
alert( 'S\u0307\u0323' == 'S\u0323\u0307' ); // false

Для решения этой проблемы существует алгоритм нормализации Unicode, который приводит каждую строку к одной форме.

Реализуется по str.normalize()

alert( "S\u0307\u0323".normalize() == "S\u0323\u0307".normalize() ); // true

Забавно, что в нашей ситуации normalize() фактически сводит последовательность из 3 символов к одному: \u1e68 (S с двумя точками).

alert( "S\u0307\u0323".normalize().length ); // 1
alert( "S\u0307\u0323".normalize() == "\u1e68" ); // true

На самом деле это не всегда так. Причина в том, что символ Ṩ достаточно распространен, поэтому создатели UTF-16 включили его в основную таблицу и дали ему код.
Если вы хотите узнать больше о правилах и вариантах нормализации, то они описаны в приложении Unicode standard: Unicode Normalization Forms.
